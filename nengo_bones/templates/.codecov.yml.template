# Automatically generated by nengo-bones, do not edit this file directly
# Version: {{ version }}

codecov:
  {% if skip_appveyor %}
  ci:
    - "!ci.appveyor.com"
  {% endif %}
  notify:
    require_ci_to_pass: no

coverage:
  status:
    project:
      default:
        enabled: yes
        target: {{ abs_target }}
    patch:
      default:
        enabled: yes
        target: {{ diff_target }}
    changes: no
